This program sorts the input values using those operations below, and prints the operations it used to sort it.
In print_check.c file you have printlist function which prints the list. Add it before free_stack function in main and you can
print the sorted list. :)
You need to unpack the src folder to use Makefile on it : p
